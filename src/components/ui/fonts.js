import { Averia_Serif_Libre } from 'next/font/google';

 export const averiaSerifLibre = Averia_Serif_Libre({
    subsets: ['latin'], // Specify the character subset
    weight: ['400','700'], // Add desired weights (400 = Regular, 700 = Bold)
});